// This function takes a number as input and returns it increased by one.
// For example, if the input is 10, the output will be 11.
const increasePage = (n) => {
  n += 1;  // Increment the number by 1
  return n; // Return the incremented value
};

export { increasePage };
